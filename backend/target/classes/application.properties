spring.application.name=backend

server.port=${PORT:8080}
server.address=0.0.0.0

spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d836t2rtqb8s73dd2hpg-a.virginia-postgres.render.com:5432/a3_validacao_token}
spring.datasource.username=${DB_USER:marcus}
spring.datasource.password=${DB_PASSWORD:wdLLToUuICxuwV2xC9T0yiTuBWjAcadL}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================================
# CONFIGURAÇÃO DO SPRING MAIL (CORRIGIDA)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=naorespondabuynow@gmail.com
# Nota: Esta senha de app deve continuar ativa no seu painel do Google
spring.mail.password=mculecjktrmgxvli

# Autenticação e Protocolo de Segurança
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false

# Segurança estrita para o ambiente do Render conseguir conversar com o Gmail
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.socketFactory.port=587
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=true

# timeouts para evitar travar a aplicação (em milissegundos)
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000