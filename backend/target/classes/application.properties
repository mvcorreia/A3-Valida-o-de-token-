spring.application.name=backend

# Volta a porta padrão fixa que o Render espera encontrar para o Java
server.port=8080

# Conexão com o Banco de Dados PostgreSQL (Render)
spring.datasource.url=jdbc:postgresql://dpg-d836t2rtqb8s73dd2hpg-a.virginia-postgres.render.com:5432/a3_validacao_token
spring.datasource.username=marcus
spring.datasource.password=wdLLToUuICxuwV2xC9T0yiTuBWjAcadL
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Desativa o starter de email nativo (assim você continua pegando o token direto no log sem travar)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration