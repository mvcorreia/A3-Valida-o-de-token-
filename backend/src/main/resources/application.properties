spring.application.name=backend

server.port=${PORT:8080}
server.address=0.0.0.0

spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d836t2rtqb8s73dd2hpg-a.virginia-postgres.render.com:5432/a3_validacao_token}
spring.datasource.username=${DB_USER:marcus}
spring.datasource.password=${DB_PASSWORD:wdLLToUuICxuwV2xC9T0yiTuBWjAcadL}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================================================================
# CONFIGURAÇÃO DO SPRING MAIL (BLINDADA PARA O RENDER)
# ===================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=naorespondabuynow@gmail.com

# Nota: Garanta que esta senha de 16 letras foi gerada dentro da conta naorespondabuynow@gmail.com
spring.mail.password=mculecjktrmgxvli

# Autenticação e Protocolo de Segurança (STARTTLS correto para a porta 587)
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Configurações de criptografia aceitas pelo Gmail e compatíveis com os containers do Render
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2 TLSv1.3
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Timeouts essenciais em milissegundos para evitar que o Render trave esperando a resposta da rede
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000